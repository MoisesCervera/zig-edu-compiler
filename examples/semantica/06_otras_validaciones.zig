// Errores independientes: constante, llamada, retorno, indice y contexto.
fn sumar(a: i32, b: i32) i32 {
    return true;
}
pub fn main() void {
    const limite = 5;
    limite = 10;
    _ = sumar(1);
    const datos = [_]u8{ 'A', 'B' };
    _ = datos[2];
    break;
}
